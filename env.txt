# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME_MGMT=omni_warehouse_mgmt
DB_NAME_REPORTS=omni_warehouse_reports

# Jenkins Integration (required for backup and provision jobs)
JENKINS_URL=http://localhost:8080
JENKINS_USER=Rahil010205
JENKINS_TOKEN=your-jenkins-api-token-here

# Authentication (if implementing real auth)
JWT_SECRET=your-jwt-secret-key

# Application Configuration
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Backup Storage (if implementing real backups)
BACKUP_STORAGE_PATH=/path/to/backup/storage